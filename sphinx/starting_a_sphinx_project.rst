Lightning start to Sphinx
=========================

Rapid tutorial::

    #Install sphinx
    $ pip install sphinx
    # Build an initial
    $ sphinx-quickstart
    # Build initial doc (assuming defaults)
    $ make html
    # Add a project
    $ vi source/conf.py

